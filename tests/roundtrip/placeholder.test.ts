import { describe, it, expect } from 'vitest';

/**
 * Placeholder so `pnpm test` is green in Phase 0.
 * Replace with real round-trip tests in Phase 2:
 *   for every fixture, serialize(parse(x)) must equal x.
 */
describe('scaffold', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
