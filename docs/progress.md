# Progress

세션 시작 시 이 파일을 읽고, 종료 시 갱신한다.

## 현재 위치

**Phase 6 — 놀이터 완성 + v0.1 공개** (Phase 0~5는 2026-08-02 완료)

테스트 196개 전부 통과. **유니티 6000.0.40f1과 대조해 케이스 17/18,
값 242/244(99.2%) 일치.** 수치와 사유는 `docs/accuracy.md`.

v0.1의 기술적 부분은 끝났다. 남은 것은 남에게 보여줄 수 있게 만드는 일이다.

## 다음 할 일

Phase 6. ROADMAP의 체크리스트 순서대로.

1. **놀이터 스테이지 배경을 바꾼다.** 샘플의 `.panel`(`rgb(40,42,48)`)과 스테이지
   (`#2b2d33`)가 3 차이라 패널 경계가 안 보인다. 데모에서 이건 손해다
2. 예제 프리셋 드롭다운 — 지금 "presets (Phase 6)" 자리표시자만 있다
3. 경고 표시 영역은 이미 붙어 있다. 노드 하이라이트까지 갈지는 선택
4. URL 공유
5. GitHub Pages 배포
6. README에 데모 GIF + **정확도 수치는 이미 넣었다**
7. `docs/uss-vs-css.md` 작성 (독립 SEO 자산)
8. npm 배포, GitHub Topics, v0.1.0 릴리스 노트

공개 전에 정직하게 밝혀야 할 것 (`docs/supported.md`에 반영해뒀다):

- `Button`은 골든 케이스에 하나도 없다. 렌더링 경로는 `Label`과 같지만 미대조다
- 텍스트 측정을 유니티와 대조하지 않았다. 행 높이 `font-size * 1.2`는 여전히 추측이다
- 색·테두리·모서리 등 시각 속성은 좌표 비교 대상이 아니라 미검증이다
- `-unity-slice-*`, `-unity-background-image-tint-color`, `transition`은 미구현이다

Phase 5가 판정해야 할 미검증 가정 (전부 ROADMAP에 적혀 있다):
`flex-shrink` 기본값 1, 우선순위 트리플 계산, 상속 목록, `:root` 의미,
행 높이 `font-size * 1.2`.

## 결정 기록

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-02 | 레이아웃을 Yoga WASM에 위임 | UI Toolkit이 같은 엔진을 쓰므로 직접 구현은 반드시 어긋남 |
| 2026-08-02 | 캔버스가 아닌 DOM 페인팅 | 텍스트·클릭 판정·접근성을 브라우저에 위임 |
| 2026-08-02 | 스타일 값에 출처 정보 포함 | Phase 8 편집 기능의 전제. 나중에 붙일 수 없음 |
| 2026-08-02 | v0.1 컨트롤 3종 고정 | 스코프 폭주가 최대 실패 요인 |
| 2026-08-02 | 빌드 모드를 env 대신 Vite `command`로 분기 | env 방식은 스크립트가 값을 안 넘겨도 조용히 잘못된 모드로 돈다 |
| 2026-08-02 | CI에 `pnpm build` 추가 | typecheck·test는 빌드 산출물이 비어도 통과한다 |
| 2026-08-02 | 왕복을 스팬 재출력으로. 노드당 스팬 3분할(`openTag`/`inner`/`closeTag`) | 안 건드린 노드는 원본을 슬라이스해 뱉으므로 **편집 없는 왕복이 바이트 일치로 공짜로 나온다.** 스팬이 하나면 자손을 하나 고칠 때 조상까지 재생성되어 사용자 들여쓰기가 루트까지 날아간다 |
| 2026-08-02 | 파생물을 모델에 저장하지 않는다 (계산된 스타일, 줄 번호, 펼친 shorthand) | 저장하면 편집 후 낡고, 낡은 걸 못 알아채면 틀린 값을 보여준다. `padding: 6px 14px`를 펼쳐 저장하면 다시 합칠 때 `6px 14px 6px 14px`가 되어 왕복이 깨진다 |
| 2026-08-02 | 출처는 이긴 것만 기록. 후보 목록은 물어볼 때 재계산 | 화면용 계산기와 같은 함수를 한 요소에 다시 돌린다. **승자 판정 로직이 하나뿐이라 두 답이 어긋날 수 없다.** 미리 쌓아두는 쪽은 USS 한 줄 수정에 무효화 추적이 필요하고, 틀리면 조용히 틀린 답을 낸다 |
| 2026-08-02 | "지원하는가"를 모델이 판단하지 않는다 | 지원 범위는 렌더러 사정이고 Phase 7에서 바뀐다. 파일은 그대로인데 모델 내용이 바뀌면 안 된다. 덕분에 컨트롤 추가 시 모델은 한 줄도 안 바뀐다 |
| 2026-08-02 | devlog를 `docs/progress.md`로 일원화 | 기록처가 둘이면 둘 다 썩는다. 세션 로그 절이 이미 그 역할이었다 |
| 2026-08-02 | 코드 규약 LCC 채택 (CLAUDE.md) | 에이전트가 함수 본문만 봐서 알 수 없는 것(외부 상태·호출 순서·**소유권**)을 드러내는 데 집중. Yoga 노드 해제 책임이 이 코드베이스 최대 함정이라 원문에 없던 소유권 항목을 넣었다 |
| 2026-08-02 | `DOMParser`를 버리고 스캐너를 직접 작성 | **DOM API는 소스 오프셋을 주지 않는다.** 표준에 없다. 스팬 재출력이 태그 부위별 오프셋을 요구하므로 양립 불가였다. 대신 우리 스캐너는 해석할 필요가 없고 위치만 찾으면 되므로 일반 XML 파서보다 훨씬 좁은 문제다 |
| 2026-08-02 | 테스트를 왕복 / 구조 / 편집 3종으로 분리 | **왕복만으로는 파서가 옳다는 증명이 안 된다.** 스팬으로 슬라이스해 뱉으면 트리가 엉망이어도 원본이 재현된다. 왕복은 스팬 장부만 검증하고, 트리 정확성은 구조 테스트가, 스팬 분할의 유용성은 편집 테스트가 본다 |
| 2026-08-02 | USS 선언 스팬에서 트레일링 `;`를 제외 | 세미콜론을 간격에 남기면 **마지막 선언에 세미콜론이 없는 경우가 특수 케이스가 아니게 된다.** 모든 선언이 같은 모양이 된다. 대가는 Phase 8에서 선언을 삭제할 때 `;`가 남는 것이고, 그건 삭제 로직이 처리한다 |
| 2026-08-02 | 형제 결합자 `+` `~`를 뒤 파트의 `unknown` 조각으로 주입 | `Combinator`에 자리를 만들면 리졸버가 매칭을 시도하게 된다. USS에 없는 기능이므로 매칭되면 안 되고, `unknown`이 있으면 규칙 전체가 버려진다는 기존 규칙에 그대로 태우는 편이 안전하다 |
| 2026-08-02 | 파싱 테스트를 `@vitest-environment node`로 | jsdom이 `import.meta.url`을 `http:` URL로 줘서 `fileURLToPath`가 거부한다. 파싱은 순수 문자열 작업이라 DOM이 애초에 불필요하고, node 환경이 더 빠르다 |
| 2026-08-02 | shorthand 전개를 리졸버에서 한다 (ROADMAP에 없던 항목) | 한 시트에 `padding`과 `padding-top`이 같이 있으면 **어느 쪽이 이기는지가 캐스케이드 질문**이다. 전개하지 않고는 답할 수 없으므로 렌더링 관심사가 아니다 |
| 2026-08-02 | 리졸버가 기본값을 채우지 않는다 | 결과 맵에 없으면 기본값이라는 규약. 모든 속성을 채우면 맵이 비대해지고, 기본값 표가 리졸버와 렌더러 두 곳에 생긴다. 표는 그것을 실제로 아는 쪽(Phase 4)이 하나만 갖는다 |
| 2026-08-02 | 인라인 스타일에 도달 불가능한 specificity를 부여 | 별도 비교 경로를 만드는 대신 `[MAX_SAFE_INTEGER, 0, 0]`을 준다. 비교 함수가 하나로 유지되고, "인라인이 항상 이긴다"가 규칙이 아니라 산술의 결과가 된다 |
| 2026-08-02 | `:root`를 `<ui:UXML>` 요소에 매칭 | USS의 `:root`는 스타일시트가 적용된 요소를 가리키는데, 프리뷰에는 UIDocument가 없다. 아무데도 매칭시키지 않으면 `:root { --token: ... }` 디자인 토큰 패턴이 통째로 깨진다. 매번 `version-dependent` 경고를 남긴다 |
| 2026-08-02 | `explainProperty`가 `collectCandidates`를 공유 | 처음엔 수집 로직을 복제해서 썼는데, 그 순간 "두 답이 어긋날 수 없다"는 Phase 1 결정이 무효가 된다. 승자 판정뿐 아니라 **후보 수집까지** 한 함수여야 성립한다 |
| 2026-08-02 | `yoga-layout` 대신 `yoga-layout/load`를 쓰고 `render`를 동기로 유지 | 기본 엔트리는 top-level await을 써서, import하는 것만으로 우리 모듈 그래프 전체가 비동기가 된다. `load`는 평범한 `async function`이라 그게 없다. `await loadLayoutEngine()`을 한 번 부르는 대가로 놀이터(타이핑마다 리렌더)와 골든 테스트가 동기 호출을 쓸 수 있다 |
| 2026-08-02 | 지원하는 레이아웃 속성을 Yoga에 전부 명시적으로 쓴다 | Yoga 기본값이 USS와 **부분적으로만** 같다. `flex-direction`(column)·`box-sizing`(border-box)은 일치하지만 `flex-shrink`는 Yoga 0 / USS 1이다. "마침 맞는" 것에 기대면 어느 게 우연인지 구분이 안 되고, Yoga 업데이트에 조용히 깨진다 |
| 2026-08-02 | Yoga enum을 import해서 쓰고 숫자를 코드에 적지 않는다 | `setPositionType(absolute)`에 0을 적을 뻔했다. 정답은 2이고 0은 Static이다. 타입 검사로도 테스트로도 안 잡히는 종류의 오류라, 상수를 손으로 옮기는 일 자체를 없앴다 |
| 2026-08-02 | `measureText`를 주입 가능하게 | 기본 구현은 캔버스를 쓰는데 결과가 플랫폼·설치 폰트에 따라 흔들린다. Phase 5가 그 위에서 정확도를 재면 라이브러리가 아니라 기계를 재게 된다 |
| 2026-08-02 | DOM을 트리 모양으로 중첩한다 (평탄화하지 않음) | 평탄화하면 `overflow: hidden` 클리핑과 겹침 순서를 직접 구현해야 한다. 중첩하면 **문서 순서가 곧 페인팅 순서**라 USS의 "뒤 형제가 위"가 공짜로 나오고, z-index를 출력할 이유가 사라진다 |
| 2026-08-02 | 페인팅 시 `data-uxml-node`를 붙인다 | Phase 8이 클릭한 div를 노드로 되짚어야 하는데, 나중에 붙이려면 페인팅을 다시 짜야 한다. 지금은 속성 하나다 |
| 2026-08-02 | 빌드에서 `yoga-layout`을 external로 | 번들에 넣으면 base64 WASM이 통째로 들어가 178KB가 된다(뺐더니 39KB). `dependencies`에 선언돼 있어 소비자가 이미 설치하므로 두 벌이 실린다 |
| 2026-08-02 | 골든 비교를 픽셀이 아니라 **좌표**로 | 유니티는 자기 폰트 에셋으로, 브라우저는 `sans-serif`로 글자를 그린다. **레이아웃이 100% 정확해도 글자 픽셀은 전부 다르다.** 텍스트가 든 케이스에서 픽셀 수치는 레이아웃이 아니라 폰트 차이를 재고, 그러면 "정확도 92%"가 무엇의 92%인지 말할 수 없게 된다 |
| 2026-08-02 | 유니티 기준값을 스크린샷 대신 좌표 JSON으로 | ROADMAP 원안은 수동 스크린샷이었다. 요소별 `worldBound`를 JSON으로 쓰는 에디터 스크립트는 배치모드 파이프라인이 아니라 스크립트 하나이고, 눈으로 좌표를 읽는 것보다 정확하며 케이스가 늘어도 비용이 그대로다 |
| 2026-08-02 | 기준값이 없는 케이스는 실패가 아니라 skip | 아무도 생성할 수 없는 파일이 없다고 CI가 깨지면 안 된다. 동시에 미측정이 통과로 보이면 더 나쁘므로, 테스트가 확보율(`0/18`)을 매번 출력한다 |
| 2026-08-02 | 케이스를 파일 40개가 아니라 `cases.ts` 하나로 | 테스트가 읽는 사본과 유니티가 읽는 사본이 갈라질 수 없다. `pnpm golden:emit`이 유니티용 파일을 찍어내고, 그 출력은 gitignore한다 |
| 2026-08-02 | 골든 케이스를 명시적 크기 위주로 짠다 | 폰트 메트릭이 좌표를 좌우하면 검증 대상(USS→Yoga 매핑)이 폰트 차이에 묻힌다. 텍스트 측정에 의존하는 2개는 `measuresText`로 표시해 비교에서 제외한다 |
| 2026-08-02 | `%` 불일치를 고치지 않고 `KNOWN_DIVERGENCES`에 등록 | `yoga-layout` 3.2.1과 UI Toolkit 6000.0.40f1이 벤더링한 Yoga의 동작 차이다. `Errata` 전 값, `useWebDefaults`, 가용 크기 변경으로 재현되지 않았다. `flex-basis: 0`은 같은 숫자를 내지만 **우연**이고 기본값으로 삼으면 명시적 높이를 가진 모든 요소가 0으로 붕괴한다. 손보정은 곧 레이아웃 개입이고 **절대 규칙 1 위반**이다 |
| 2026-08-02 | 알려진 불일치를 허용 오차가 아니라 **목록**으로 관리 | 오차를 늘리면 그 안에서 다른 불일치가 생겨도 안 보인다. 목록은 정확히 일치해야 하므로, 불일치가 사라지거나 번지면 테스트가 실패한다 |

## 세션 로그

### 2026-08-02
- 저장소 생성, 기반 문서 작성
- Phase 0 DoD 전 항목 검증 완료 (typecheck / test / build / dev 서버 실제 실행)
- **빌드 설정 버그 수정.** `vite.config.ts`가 `LIB_BUILD` 환경변수로 모드를
  나눴는데 이 값을 넘기는 스크립트가 없었다. 라이브러리 빌드가 root를
  `playground/`로 둔 채 돌았고, `vite-plugin-dts`가 `playground/src`를 찾다
  실패한 뒤 **성공 로그만 남기고 `.d.ts`를 하나도 내지 않았다.**
  `package.json`의 `types`가 없는 파일을 가리키던 상태 — npm 배포(Phase 6)
  시점에 터졌을 문제다. Vite의 `command`로 분기하고 경로를 전부 절대경로로
  바꿔서 해결. 재발 방지로 CI에 `build`를 넣었다.
- `pnpm test:golden`이 `tests/golden/`이 없어 exit 1이던 것 수정
  (`--passWithNoTests`). Phase 5에서 실제 테스트가 들어온다.
- (18:19 추가) `.gitattributes` 신규. `core.autocrlf=true`인데 `.gitattributes`가
  없어서 git이 체크아웃 시 LF를 CRLF로 바꾸고 있었다. 파서가 읽는 바이트가 커밋된
  바이트와 달라지면 Phase 2 왕복 테스트가 플랫폼마다 다르게 나온다.
  `* text=auto eol=lf` + 픽스처는 `-text`로 제외 (CRLF 파일의 왕복도 테스트 대상이라
  git이 손대면 안 된다). `git add --renormalize`로 기존 파일에 줄바꿈 변경이 없음을 확인.
- `/devlog` 스킬 작성 (`.claude/skills/devlog/SKILL.md`). 다른 프로젝트의
  devlog-writer를 옮기면서 파일 탐색 절차와 유니티 잔재를 걷어내고, progress.md의
  절별 갱신 방식(덮어쓰기 / append)을 명시했다. `allowed-tools`에서 `Write`를 빼서
  append-only를 도구 권한으로 강제한다 — 지시문으로 부탁하는 것보다 확실하다.
- CLAUDE.md에 코드 규약 LCC 추가, 세션 규칙을 `/devlog`에 연결.
- **Phase 1 완료.** `src/model/types.ts` 작성. `src/index.ts`는 모델 타입을 재수출하고
  `Warning`에서 `line`을 빼고 `at: SourceRef` / `node: NodeId`로 교체했다.
  typecheck / test / build 전부 exit 0, `dist/model/types.d.ts` 생성 확인.
- 타입을 쓰다가 발견한 것 둘. 논의만으로는 안 나왔고, 실제로 적어보니 드러났다.
  - **dirty 플래그 하나로는 부족하다.** 자식 목록이 바뀌면 무효가 되는 건 여는
    태그가 아니라 *자식 사이의 공백*이다. 서로 다른 사건이라 `tagDirty`와
    `childrenDirty`로 나눴다. 하나로 뭉치면 자식 하나 삽입에 여는 태그까지
    재생성되어 서식이 날아간다
  - **루트 바깥 텍스트의 자리가 없었다.** XML 선언과 첫머리 주석이 갈 곳이 없었는데,
    `source`에서 루트 스팬 바깥을 슬라이스하면 되므로 필드를 추가하지 않고
    주석으로 명시했다. 파생물은 저장하지 않는다는 원칙이 여기에도 걸린다
- 다음에 바꿀 것
  - `Warning`에 줄 번호가 없어서, 놀이터가 에디터에 위치를 표시하려면 스팬 → 줄·열
    변환 유틸이 필요하다. Phase 2에서 만든다
  - 인라인 `style` 속성의 선언을 노드에 저장하지 않기로 했다. 리졸버가 매번 파싱하는데,
    실측해서 느리면 캐시를 붙인다. 단 모델이 아니라 리졸버 쪽에 붙인다
- (20:14 추가) **전체 계획을 점검하다 낡은 전제 네 개를 발견했다.** Phase 1 결정이
  앞 문서들의 가정을 바꿔놨는데 문서가 안 따라와 있었다.
  - `architecture.md`가 L1을 `DOMParser`로 적어뒀다 → 소스 오프셋을 안 주므로 불가능.
    직접 스캐너로 바꾸고 결정 3-b를 추가했다
  - `ROADMAP.md`가 "바이트 일치는 과한 목표일 수 있다"고 적어뒀다 → 스팬 재출력에서는
    정의상 바이트 일치다. 목표를 낮출 게 아니라 안 나오면 파서가 틀린 것이다
  - DoD가 "UXML 파서 (VisualElement, Label, Button)"였다 → 파서는 컨트롤을 구분하지 않는다
  - DoD에 구조 테스트가 없었다 → 왕복만으로는 트리가 맞는지 증명되지 않는다
- **Phase 2 완료.** 픽스처 15개, 스캐너 2개(`src/parser/`), 직렬화 2개(`src/serializer/`),
  테스트 65개. `pnpm build`가 처음으로 실제 번들(14KB)을 낸다.
- **왕복이 첫 실행에 15/15 바이트 일치로 통과했다.** 주석(루트 앞뒤·요소 사이·같은 줄·
  여러 줄), CRLF, 엔티티 두 표기(`&amp;` / `&#38;`), 작은따옴표, 여러 줄에 걸친 속성,
  뒤섞인 탭/스페이스, 끝 개행 없음, 미지원 컨트롤이 전부 원본 그대로 나온다.
  스팬 재출력 설계가 실제로 작동한다.
- 직렬화의 핵심은 `emitInner`다. 자식과 **자식 사이의 간격**을 원본에서 번갈아
  슬라이스하므로, 아무것도 안 고쳤을 때 결과가 원본과 같아지는 것이 특수 케이스가
  아니라 자동이다. USS도 같은 구조다.
- 편집 테스트로 확인한 것
  - 속성 하나를 고치면 **그 태그 바깥은 한 바이트도 안 바뀐다.** 다만 태그 안에서는
    속성 전부가 재생성되어 여러 줄 속성이 한 줄로 접힌다
  - 자식 하나를 지우면 남은 형제들은 바이트 그대로 유지된다
  - `childrenDirty`가 켜지면 **자식 사이의 주석이 사라진다.** 주석이 간격 안에서만
    살아남기 때문이다. 테스트로 명시해뒀다 (`tests/edit/edit.test.ts`)
- 다음에 바꿀 것
  - `Attribute`에 `dirty`를 추가하고 속성 사이 간격도 슬라이스하면 위 첫 번째 한계가
    사라진다. Phase 8 전에 한다
  - `childrenDirty`의 주석 유실은 간격을 형제로 승격시켜야 풀린다. 구조 변경이라
    Phase 8에서 편집 UI와 함께 설계한다
- (21:02 추가) **Phase 3 착수 전 의존성 점검에서 막히는 지점 셋을 먼저 뚫었다.**
  코드를 쓰기 시작했으면 중간에 발이 묶였을 것들이다.
  - 인라인 `style` 속성을 파싱할 경로가 없었다. `parseDeclarations`가 `parseUss`
    내부 클로저였다. `parseDeclarationList`로 승격시켜 규칙 블록과 인라인이 같은
    문법 파서를 쓰게 했다. 리팩터링 후 기존 65개 그대로 통과
  - `@import`을 담기만 하고 불러올 경로가 없었다. `parse`에 `resolveImport` 훅 추가.
    모델이 이미 `sheets[]`였던 덕에 타입 변경은 없었다
  - 속성 값의 시작 오프셋을 모델이 안 들고 있었다. `Attribute.span.end - 1 - value.length`로
    산술 유도가 가능해서 필드를 늘리지 않았다 (`attributeValueStart`)
- **Phase 3 완료.** `src/style/` 4개 파일, 캐스케이드 테스트 50개, 전체 117개.
  `specificity.ts` / `selector.ts` / `properties.ts` / `resolve.ts`로 나눴다.
- 구현 중 스스로 잡은 것: `explainProperty`를 처음에 **후보 수집 로직을 복제해서**
  썼다. 그 순간 "화면용과 설명용이 같은 함수라 어긋날 수 없다"는 Phase 1 결정이
  깨진다. `collectCandidates` 하나로 합쳐서 고쳤다. 결정이 코드에서 지켜지는지는
  결정을 적어두는 것만으로는 보장되지 않는다.
- 셀렉터 매칭은 **오른쪽에서 왼쪽으로 가면서 자손 결합자에서 백트래킹**한다.
  `.a .b .a .b`가 걸리는 트리에서 백트래킹 없이는 실패한다. 테스트로 고정했다.
- 다음에 바꿀 것 / 확인할 것
  - **우선순위 트리플 계산과 pseudo-class를 class로 세는 규칙은 검증되지 않았다.**
    문서화된 순서에서 역산했을 뿐이다. Phase 5 골든 테스트가 동률에서 어긋나면
    `src/style/specificity.ts`를 먼저 본다
  - 상속 목록(`src/style/properties.ts`의 `INHERITED`)이 버전 의존이다. 한 곳에 모아뒀다
  - `transition`은 전개하지 않고 통째로 둔다. 콤마 목록이라 longhand도 목록이고,
    Phase 4 전까지 읽는 곳이 없다
- (21:22 추가) **Phase 4 착수 전 실측이 계획을 세 군데 바꿨다.** 문서만 읽고
  시작했으면 전부 코드를 쓴 뒤에 발견했을 것들이다.
  - `render()`가 동기로 선언돼 있는데 `yoga-layout` 기본 엔트리는 top-level await을
    쓴다. `yoga-layout/load`에는 TLA가 없다는 걸 확인하고 그쪽을 경유했다
  - Yoga 기본값을 실제로 찍어봤다. `flexDirection=0(Column)`·`boxSizing=0(BorderBox)`은
    USS와 일치하지만 **`flexShrink=0`은 USS 문서값 1과 다르다.**
    "마침 맞음"에 기대지 않기로 하고 지원 속성을 전부 명시적으로 쓴다
  - `getInstanceCount`가 JS 바인딩에 없다. 누수를 Yoga에게 물어볼 수 없어서
    `liveNodeCount()`로 직접 센다
- **`setPositionType`에 0을 적을 뻔했다.** `absolute`는 2이고 0은 `Static`이다.
  모든 절대위치 요소가 조용히 잘못 배치됐을 것이고 타입 검사로도 안 잡힌다.
  enum 파일을 열어 값을 확인한 뒤, 하드코딩한 숫자를 전부 지우고 import로 바꿨다.
- **Phase 4 완료.** `src/render/`(values·css-map·measure·paint), `src/layout/yoga.ts`,
  `src/controls/registry.ts`. 레이아웃 테스트 23개 + 페인팅 테스트 17개, 전체 157개.
  놀이터도 실제 렌더에 배선했다.
- 테스트가 잡아준 착각: Label 너비가 200으로 나와서 측정이 깨진 줄 알았는데,
  **`align-items: stretch`(기본값)가 교차축을 늘리는 게 맞았다.** 코드가 아니라
  기대값이 틀렸다. 주축(높이)과 `align-self: flex-start`인 경우로 나눠 고정했다.
- 빌드 산출물이 178KB였다. Yoga의 base64 WASM이 통째로 인라인된 것이라
  external로 빼서 39KB가 됐다.
- 다음에 바꿀 것 / 확인할 것
  - **브라우저 육안 확인이 아직이다.** jsdom과 dev 서버 200까지만 봤다
  - 행 높이를 `font-size * 1.2`로 잡았다. 유니티와 다를 가능성이 높다
  - 미지원 컨트롤은 문서대로 하위 트리째 렌더에서 제외한다. 프리뷰 용도로는
    일반 `VisualElement`처럼 그려주는 편이 나을 수 있어 backlog에 올렸다
- (22:06 추가) **브라우저 육안 확인 완료.** Phase 4의 마지막 미확인 항목이 닫혔다.
  놀이터 샘플이 의도대로 나왔다 — 버튼 가로 배치, 제목이 위, 버튼 폭이 글자 수에 따라
  다름(캔버스 측정이 실제로 돎), 버튼 높이 동일(`align-items: stretch`가 교차축에 적용),
  경고 없음. **브라우저에서 WASM 인스턴스화가 실제로 된다는 것**이 이걸로 확인됐다.
- `.row`의 `flex-direction: row;`를 지우니 버튼이 세로로 쌓였다. CSS였다면 가로로
  남았을 자리다. **1순위 실패 패턴(기본값 `column`)이 제대로 재현된다.**
- **사용자가 자신의 졸업작품 UXML/USS를 넣어 확인했고 프리뷰가 제대로 나왔다.**
  이쪽이 우리 픽스처보다 강한 증거다 — 손으로 만든 케이스는 만든 사람이 아는 문법만
  담고, 실사용 파일은 그렇지 않다.
- 놀이터 관찰: 샘플의 `.panel` 배경 `rgb(40, 42, 48)`과 스테이지 배경 `#2b2d33`
  (= `rgb(43, 45, 51)`)이 3 차이라 패널 경계가 눈에 안 보인다. 버그는 아니지만
  데모 페이지로는 손해다. Phase 6에서 스테이지 배경을 바꾼다.
- (22:18 추가) **Phase 5 하네스 완성.** 케이스 20개(`tests/golden/cases.ts`),
  결정론적 하네스(`harness.ts`), 회귀 + 정확도 2단 테스트(`golden.test.ts`),
  유니티 추출 스크립트(`tools/UxmlLayoutDump.cs`), `docs/accuracy.md`.
  전체 196개 중 178개 통과, 18개는 기준값 없어 skip.
- **골든 테스트가 두 가지를 섞고 있었다.** "우리 출력이 바뀌었나"(회귀)와
  "유니티와 같나"(정확도)는 필요한 것도 실패 의미도 다르다. 회귀는 브라우저도
  유니티도 없이 CI에서 상시 돌고, 정확도는 기준값이 있을 때만 돈다.
- **스냅샷을 훑다가 설명 안 되는 숫자를 찾았다.** `percent-without-parent-size`에서
  크기 미지정 부모의 자식이 `width: 50%` → 0인데 `height: 50%` → 75(부모 150)로
  나왔다. Yoga를 직접 찍어 확인: **축마다 다르다.** 주축은 내려온 가용 공간에
  대해 풀리고 교차축은 0이 된다. 형제가 없어도 같다.
  CLAUDE.md의 "`%`는 부모에 명시적 크기가 없으면 계산되지 않는다"는 가로에만 맞다.
  **고치지 않았다** — 유니티도 같은 Yoga를 쓰므로 같은 값이 나와야 하고, 그렇다면
  코드가 아니라 문서를 고쳐야 한다. 케이스 질문에 이 축 비대칭을 명시했다.
- 실수 둘을 스스로 잡았다.
  - `UxmlLayoutDump.cs`를 처음에 리플렉션으로 private `UpdateLayout`을 호출하게 썼다.
    존재를 확인할 수 없는 API를 추측한 것이라, 문서화된 `GeometryChangedEvent`를
    기다리는 EditorWindow 방식으로 다시 썼다
  - `package.json`에 `cross-env-shim`이라는 없는 패키지를 적었다. 윈도우에서
    `VAR=1 cmd` 문법이 안 되는 문제인데, 의존성을 늘리지 않고
    `scripts/update-golden.mjs`가 `spawnSync`로 환경변수를 넘기게 했다
- Node 22.17이 `.ts` 동적 import를 기본으로 못 읽어서 `golden:emit`에
  `--experimental-strip-types`를 붙였다.
- 다음에 바꿀 것
  - **`UxmlLayoutDump.cs`는 유니티에서 실행해본 적이 없다.** 컴파일 오류나
    좌표 0 문제가 나올 수 있다
  - `pixelmatch`·`pngjs`가 아직 미사용이다. 색·테두리 같은 시각 속성 대조에
    쓸 자리가 남아 있다
- (23:26 추가) **유니티 대조 완료. 케이스 17/18, 값 242/244(99.2%) 일치.**
  Unity 6000.0.40f1, `yoga-layout` 3.2.1, 패널 400×300, 허용 오차 0.5px.
  일치한 17개 케이스 59개 요소는 **오차 0**이었다.
- **미검증 가정 네 개가 전부 "맞음"으로 판정됐다** — `flex-shrink` 기본값 1,
  우선순위 트리플 계산, 직접 지정이 상속을 이김, 인라인 최우선.
  `flex-shrink`는 Yoga 엔진 기본값이 0이라 그대로 뒀으면 틀렸을 자리다.
  Phase 4의 "지원 속성을 전부 명시적으로 쓴다"가 정확히 이걸 막았다.
- **불일치 1건. 유니티가 옳고 우리가 틀렸다.** 크기 미지정 부모에 대한 `%`에서
  유니티는 두 축 모두 0, 우리는 세로가 150/75다. CLAUDE.md의 서술이 맞았고,
  "문서가 지나친 단순화일 수 있다"던 내 추측이 틀렸다.
  맞추려고 시도한 것과 결과를 전부 `docs/accuracy.md`에 남겼다 —
  가용 크기 변경, `Errata` 전 값, `useWebDefaults`, `flex-basis: 0`.
  마지막 것은 유니티와 같은 숫자를 냈지만 **우연**이었다.
  기본값으로 삼으면 `height: 40px`인 요소가 전부 0으로 붕괴한다.
  1개를 맞추려고 17개를 깨는 셈이라 고치지 않았다.
- 사용자가 유니티에서 실행하다 폴더 구조가 달라 `No .uxml files` 팝업을 만났다.
  스크립트가 **어디를 봤는지 안 알려준 것**이 문제라 절대경로·존재 여부·작업
  디렉터리를 항상 표시하게 고치고 Browse 버튼과 재귀 검색을 넣었다.
  그 김에 **두 번째 버그**도 잡았다 — 시퀀싱을 `GeometryChangedEvent`에 걸었는데
  스테이지가 400×300 고정이라 이벤트가 첫 케이스에만 온다. 두 번째 케이스에서
  아무 오류 없이 멈췄을 것이다. 스케줄러 기반으로 바꿨다.
- `docs/supported.md`를 실측 기준으로 갱신했다. **`검증됨`은 유니티와 좌표를
  대조한 것만**이다. 색·테두리·폰트는 코드가 있어도 `코드 작성`으로 남긴다.
- 다음에 바꿀 것 / 남은 구멍
  - **`Button`이 골든 케이스에 하나도 없다.** 렌더링 경로는 `Label`과 같지만 미대조다
  - 텍스트 측정을 유니티와 대조하지 않았다. 행 높이 `font-size * 1.2`는 추측이다
  - 시각 속성(색·테두리·모서리)은 좌표로 알 수 없다. `pixelmatch`가 쓰일 자리다
  - 다른 Unity 버전에서 재측정하면 `accuracy.md` 표를 덮어쓰지 말고 버전별로 나란히 둔다
