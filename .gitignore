node_modules/
dist/
.vite/
coverage/

# golden test output (diff images are generated, not committed)
tests/golden/output/

# emitted from tests/golden/cases.ts by `pnpm golden:emit`, for loading into
# Unity. Derivable, so not committed — a stale copy here would be a second
# source of truth for the case set.
tests/golden/cases/

.DS_Store
Thumbs.db
*.log
.env
.env.local
.idea/

# Share the project's editor setup, but not per-user VS Code state.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
